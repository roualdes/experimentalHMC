ehmc_dir = py.get_install_dir() / 'experimentalhmc'

shared_library('ehmc',
               'ehmc.cpp',
               include_directories: incdir,
               install: true,
               install_dir: ehmc_dir,
               name_suffix: 'so')